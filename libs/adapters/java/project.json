{
  "name": "java",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/adapters/java/src",
  "projectType": "library",
  "tags": ["scope:oss-core", "type:adapter"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/adapters/java",
        "main": "libs/adapters/java/src/index.ts",
        "tsConfig": "libs/adapters/java/tsconfig.lib.json",
        "assets": ["libs/adapters/java/*.md"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --pretty false --noEmit -p libs/adapters/java/tsconfig.lib.json"
      }
    }
  }
}
