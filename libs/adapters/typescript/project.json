{
  "name": "typescript",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/adapters/typescript/src",
  "projectType": "library",
  "tags": ["scope:oss-core", "type:adapter"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/adapters/typescript",
        "main": "libs/adapters/typescript/src/index.ts",
        "tsConfig": "libs/adapters/typescript/tsconfig.lib.json",
        "assets": ["libs/adapters/typescript/*.md"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --pretty false --noEmit -p libs/adapters/typescript/tsconfig.lib.json"
      }
    }
  }
}
