{
  "name": "rust",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/adapters/rust/src",
  "projectType": "library",
  "tags": ["scope:oss-core", "type:adapter"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/adapters/rust",
        "main": "libs/adapters/rust/src/index.ts",
        "tsConfig": "libs/adapters/rust/tsconfig.lib.json",
        "assets": ["libs/adapters/rust/*.md"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --pretty false --noEmit -p libs/adapters/rust/tsconfig.lib.json"
      }
    }
  }
}
