{
  "name": "sql",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/adapters/sql/src",
  "projectType": "library",
  "tags": ["scope:oss-core", "type:adapter"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/adapters/sql",
        "main": "libs/adapters/sql/src/index.ts",
        "tsConfig": "libs/adapters/sql/tsconfig.lib.json",
        "assets": ["libs/adapters/sql/*.md"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --pretty false --noEmit -p libs/adapters/sql/tsconfig.lib.json"
      }
    }
  }
}
