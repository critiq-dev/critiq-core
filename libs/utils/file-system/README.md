# @critiq/util-file-system

Shared file-system helpers for the Critiq workspace. Utility packages sit at the bottom of the dependency graph so they remain lightweight and reusable.

Allowed dependencies: `type:util`

Commands:

- `npm run nx -- build file-system`
- `npm run nx -- test file-system`
- `npm run nx -- lint file-system`
