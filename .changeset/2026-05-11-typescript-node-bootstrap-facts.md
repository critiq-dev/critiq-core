---
"@critiq/cli": patch
---

Extend the TypeScript adapter public security collectors with Fastify public listen posture without `trustProxy`, Apollo Server dev tooling plugin exposure heuristics, GraphQL multipart upload posture when Apollo CSRF is explicitly disabled, Nuxt `runtimeConfig.public` secret-shaped keys, and Astro `vite.define` wiring of secret-like `process.env` values into `import.meta.env.PUBLIC_*` keys.
